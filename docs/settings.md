# Settings

This document has moved. See [configuration.md](configuration.md) for the unified `settings.json` schema (agent directories, skill directories, server tuning, memory, terminal backend, MCP Apps, network, auth, logging) and the `cao config` CLI.
